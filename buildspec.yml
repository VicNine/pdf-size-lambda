version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
    - echo "Installing Dependencies..."
    - pip install -r requirements.txt -t lib
    build:
      commands:
      - echo "Building project..."
      -
